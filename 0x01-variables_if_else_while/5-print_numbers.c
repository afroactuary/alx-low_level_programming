#include <stdio.h>

/**
 * main - Prints all single numbers from 0-9
 *
 * Return: Always (Success)
*/
int main(void)
{
	char n;

	  (n = '0'); (n) = '9'; n++);
	{
		putchar(n);
	}

	putchar('\n');

	return (0);
}
